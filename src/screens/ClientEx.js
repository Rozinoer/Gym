import React from 'react';
import { Button, StyleSheet, View, Text } from 'react-native';
import { ClientNavbar } from '../components/ClientNavbar'

export const ClientEx = (({ navigation }) => {
    return (
        <View>
             
        </View>
    )
})